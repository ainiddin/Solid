version: "3.8"
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env

  frontend:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - "./frontend:/usr/share/nginx/html:ro"
    depends_on:
      - backend
